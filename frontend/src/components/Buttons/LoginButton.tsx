import { FiLogIn } from "react-icons/fi";

const LoginButton = () => {
  return (
    <button className="btn btn-ghost">
      Login
      <FiLogIn size={18} />
    </button>
  );
};

export default LoginButton;
